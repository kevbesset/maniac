export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  LINK = 'a',
  ROUTER_LINK = 'RouterLink',
}

export enum ButtonTheme {
  DISCRET = 'discret',
  PRIMARY = 'primary',
}

export enum ButtonSize {
  SMALL = 'small',
  LARGE = 'large',
}
